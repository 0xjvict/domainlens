## Codebase Patterns
- Build: `npm run typecheck` (tsc --noEmit) is the quality gate — no test suite exists
- Module system: ESM (`"type": "module"` in package.json) — all imports must use `.js` extension even for `.ts` source files
- Config: DomainLensConfig read from `.domainlens/config.json` in project root; projectPath always resolved to absolute path
- OpenRouter LLM: src/llm/openrouter.ts wraps openai npm package with custom baseURL; uses llm_key_env from config to resolve env var name → actual key
- Skills on disk: `<projectPath>/skills/domain/*.md` and `<projectPath>/skills/rules/*.md`
- Frontmatter parsing: src/utils/frontmatter.ts handles YAML frontmatter in skill files
- ORM scanner: src/extractors/ormScanner.ts auto-detects Prisma/Django/Laravel by indicator files; config.orm overrides detection; Laravel model path is configurable via config.laravel_model_paths (array, default ["app/Models"])
- Embedding DB: SQLite at `.domainlens/embeddings.db` using sqlite-vec extension; embeddings/embed.ts manages incremental hash-based updates
- MCP server: src/mcp/server.ts exposes search_schema, search_semantic, list_skills, get_skill via stdio transport

---

## 2026-06-12 - US-027
- Created src/agent/tools.ts with five tool handlers: list_directory, glob_files, read_file, search_text, read_schema
- Exported getToolDefinitions() returning OpenAI-compatible tool definition array
- Exported createToolHandlers(config, projectPath) returning a map of tool name → handler
- All tools respect config.ignore list; glob uses custom globToRegex supporting * and **
- read_file returns up to 500 lines with an optional offset for pagination; includes line count header
- search_text is case-insensitive, capped at 100 results; accepts optional path to restrict scope
- read_schema reads .domainlens/schemas/latest.json, returns '{}' if not found
- **Learnings for future iterations:**
  - Tools are split: getToolDefinitions() (static schema) vs createToolHandlers() (runtime closures with config/projectPath)
  - isIgnored() helper mirrors codeScanner.ts pattern: checks relativePath.startsWith(ig) || name === ig
  - glob ** handling: replace **/ → (?:.+/)? and /** → (?:/.+)? before replacing single *
  - Binary file detection uses \x00 null byte check (same pattern as codeScanner.ts)
---

## 2026-06-12 - US-026
- Added Signal and AgentConcept interfaces to src/types.ts
- Added explorer_model, agent_max_files, agent_max_context_tokens to DomainLensConfig type
- Updated DEFAULT_CONFIG in src/commands/init.ts to include explorer_model ('anthropic/claude-sonnet-4-6'), agent_max_files (150), agent_max_context_tokens (100000)
- **Learnings for future iterations:**
  - Types are the foundation — US-027 onward can import AgentConcept/Signal from types.ts
  - init.ts DEFAULT_CONFIG is the single source of truth for all config.json defaults
---
