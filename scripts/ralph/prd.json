{
  "project": "DomainLens",
  "branchName": "ralph/domainlens-v2",
  "description": "DomainLens v2 — LLM pre-scan, structured enrichment, business rules extraction, relations synthesis, 3 new MCP tools, incremental rebuild/watch, agent mode extensions, and CLI polish",
  "userStories": [
    {
      "id": "US-001",
      "title": "Add new config fields and restructure skill output directories",
      "description": "As a developer configuring DomainLens, I want the config type and init defaults to include rules_paths, rules_batch_size, and watch_interval_seconds, so that business rule extraction and watch behavior are configurable. I also want technical rules output to move from skills/rules/ to skills/rules/technical/ to separate them from business rules.",
      "acceptanceCriteria": [
        "DomainLensConfig type in src/types.ts includes rules_paths, rules_batch_size, watch_interval_seconds",
        "init command generates config.json with the new fields and correct defaults",
        "Technical rules (naming-conventions, soft-delete, performance) are written to skills/rules/technical/ instead of skills/rules/",
        "Business rules still use skills/rules/business/ (no change needed, path is new)",
        "All internal references to skill paths are updated consistently",
        "Typecheck passes"
      ],
      "priority": 1,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-002",
      "title": "Implement LLM pre-scan for implicit concept discovery",
      "description": "As a DomainLens user, I want the discover pipeline to run an LLM pre-scan step before code scanning, using the file tree + schema summary to discover implicit business concepts (like RecurringOrder, ChurnedCustomer) that have no direct table in the database. The pre-scan output must be merged with heuristic-derived concepts.",
      "acceptanceCriteria": [
        "New LLM pre-scan function sends file tree + schema summary to llm_model and returns JSON array of concept names",
        "Pre-scan runs as step 3 in discover pipeline, before code scanner",
        "Suprimido por --no-enrich",
        "Entity merge algorithm implemented: normalized name comparison, heuristic-signaled concepts win, LLM-only concepts get source: ai-inferred with signals: []",
        "Merged list feeds into enrichment step",
        "Typecheck passes"
      ],
      "priority": 2,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-003",
      "title": "Split enrichment into structured enrichDomainConcept and enrichBusinessRule",
      "description": "As a DomainLens user, I want the generic enrichConcept function replaced by two separate structured functions: enrichDomainConcept (with fixed sections: Definition, States & Lifecycle, Business Rules, Related Concepts, Common Query Patterns) and enrichBusinessRule (with sections: Rule Description, Trigger, Conditions, Effect, Where Enforced). Both use max_tokens 1500 and generate semantically-derived tags.",
      "acceptanceCriteria": [
        "enrichDomainConcept() exists with structured prompt, fixed sections, max_tokens 1500",
        "enrichBusinessRule() exists with structured prompt, fixed sections, max_tokens 1500",
        "Tags are generated semantically by LLM, not derived from filename",
        "Old generic enrichConcept is removed or no longer used for domain/rules",
        "Domain skills output uses the v2 markdown format with all 5 sections",
        "Business rule skills output uses the FR-006 markdown format",
        "Re-execution behavior preserved: new skills get full content, existing get ## Detected Changes appended",
        "--no-enrich and --dry-run flags work with both functions",
        "Typecheck passes"
      ],
      "priority": 3,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-004",
      "title": "Implement business rules extraction pipeline",
      "description": "As a DomainLens user, I want the discover pipeline to automatically extract real business rules from service, validator, and policy files. The pipeline should have a deterministic first pass (regex candidate detection based on business vocabulary and conditional density) followed by a batch LLM extraction via extractBusinessRules() that produces structured JSON rules. Duplicate rules across batches should be merged by name with accumulated enforced_in paths.",
      "acceptanceCriteria": [
        "Deterministic candidate detection scans rules_paths for high-density business conditionals",
        "Candidate files are sent to extractBusinessRules() in batches of rules_batch_size",
        "extractBusinessRules() returns structured JSON with name, description, trigger, conditions, effect, enforced_in, related_concepts",
        "Duplicate rules across batches are merged by appending enforced_in values",
        "Each unique rule passes through enrichBusinessRule() to produce the .md file",
        "Business rules are written to skills/rules/business/",
        "Integration into discover pipeline after domain enrichment",
        "Typecheck passes"
      ],
      "priority": 4,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-005",
      "title": "Implement relations synthesis (relations.md)",
      "description": "As a DomainLens user, I want the discover pipeline to generate a relations.md file that connects all domain concepts and business rules in a navigable knowledge map. The file should have 4 fixed sections: Domain Map, Concept Relations, Cross-Concept Rules, and Entry Points. It must be fully regenerated each time (never appended).",
      "acceptanceCriteria": [
        "generateRelationsMap() function exists and reads all skills/domain/*.md and skills/rules/business/*.md",
        "Produces skills/domain/relations.md with exactly 4 sections: Domain Map, Concept Relations, Cross-Concept Rules, Entry Points",
        "Fully regenerated each time (not appended) — user can set source: human to skip",
        "Runs at end of discover pipeline after enrichment and business rules",
        "Can run standalone via --relations-only flag (handled in US-012)",
        "Typecheck passes"
      ],
      "priority": 5,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-006",
      "title": "Add 3 new MCP tools: get_concept, get_rules_for_concept, get_relations",
      "description": "As an AI agent using DomainLens via MCP, I want three new structured navigation tools: get_concept returns frontmatter + parsed sections (no raw markdown), get_rules_for_concept filters business rules by related_concepts in frontmatter (authoritative source), and get_relations returns the relevant portion of relations.md for a given concept. All tools should return structured not-found responses instead of errors.",
      "acceptanceCriteria": [
        "get_concept(name) returns { name, type, source, tags, sections: Record<string, string> } or { name, found: false } if not found",
        "get_rules_for_concept(concept, limit?) filters by related_concepts frontmatter, returns array of matching rules or empty array if none",
        "get_relations(concept) returns { concept, direct_relations[], cross_concept_rules[] } or empty arrays if not found",
        "get_relations filters relations.md content for concept-specific relations and cross-concept rules",
        "All new tools registered in MCP server alongside existing 4 tools (total 7)",
        "Not-found responses are structured (not errors) for all three tools",
        "Typecheck passes"
      ],
      "priority": 6,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-007",
      "title": "Extend agent mode interface and enrichment skip logic",
      "description": "As a developer using --agent mode, I want the AgentConcept interface to include optional fields for states, business_rules (flat string array), and related_concepts. When the agent returns a concept with these fields populated, enrichDomainConcept should be skipped for that concept. The agent's initial context should only show domain and business_rule skills.",
      "acceptanceCriteria": [
        "AgentConcept interface extended with states?: string[], business_rules?: string[], related_concepts?: string[]",
        "Agent runner passes these fields through from finish() to the pipeline",
        "Enrichment skip logic: if concept has at least one optional field populated, skip enrichDomainConcept",
        "Agent initial context filters existing skills to only domain and business_rule types",
        "Concepts without optional fields still go through enrichDomainConcept normally",
        "Typecheck passes"
      ],
      "priority": 7,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-008",
      "title": "Implement file-concept-map v2 with expanded format",
      "description": "As a developer using incremental rebuild, I want the file-concept-map.json to use an expanded format with version, lastProcessed timestamp, and per-concept type information. This enables the incremental rebuild engine to know exactly which enrichment function to call for each affected concept.",
      "acceptanceCriteria": [
        "File-concept-map uses v2 format: { version: 2, files: { [path]: { lastProcessed, concepts: [{ name, type }] } } }",
        "Write and read utility functions for the map",
        "Map is updated at end of each discover run",
        "Old v1 format is not read (breaking change is acceptable for v2)",
        "Listed in .gitignore generated by init",
        "Typecheck passes"
      ],
      "priority": 8,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-009",
      "title": "Implement incremental rebuild engine via git diff",
      "description": "As a DomainLens user, I want the system to detect changes via git diff HEAD (staged + unstaged) and git ls-files --others (untracked), cross-reference with file-concept-map, and selectively re-enrich only affected concepts. Deleted files should log a warning and stamp the concept with 'source file deleted' without auto-removing it. Git-unavailable environments should log a clear fallback message.",
      "acceptanceCriteria": [
        "Detects modified files via git diff HEAD --name-only",
        "Detects new untracked files via git ls-files --others --exclude-standard",
        "Cross-references changed files with file-concept-map to identify affected concepts and their types",
        "Selective re-enrichment calls correct function (enrichDomainConcept or enrichBusinessRule) per concept type",
        "Deleted source files log warning and append 'source file deleted' to concept's Detected Changes",
        "No git available: logs ⚠ message with manual fallback instruction",
        "Re-generates relations.md if any concept is updated",
        "Re-embeds only changed files on hash check",
        "Typecheck passes"
      ],
      "priority": 9,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-010",
      "title": "Implement domainlens watch command",
      "description": "As a DomainLens user, I want a watch command that runs the incremental rebuild in a loop with configurable polling interval (default 30s). It should support --no-enrich for cost-limited environments. If filesystem events are available via chokidar, use them instead of polling as a fallback.",
      "acceptanceCriteria": [
        "domainlens watch command exists and runs incremental rebuild in a polling loop",
        "Polling interval configurable via watch_interval_seconds (default 30s)",
        "--no-enrich flag suppresses LLM calls, only updates detected signals",
        "Output shows: changed files, affected concepts, enrichment results, relations regeneration, re-embedding",
        "Uses chokidar for filesystem events if available, falls back to polling",
        "Graceful error handling: failed cycle logs error and continues",
        "Typecheck passes"
      ],
      "priority": 10,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-011",
      "title": "Update embedding pipeline to include business rules and relations",
      "description": "As a DomainLens user, I want business rules (one excerpt per file) and relations.md (split by section into 4 excerpts) to be vectorized in the embedding pipeline alongside existing content. Hash-based incremental updates should apply to the new content types.",
      "acceptanceCriteria": [
        "Each business rule file in skills/rules/business/*.md generates one embedding excerpt",
        "relations.md generates 4 excerpts (one per section: Domain Map, Concept Relations, Cross-Concept Rules, Entry Points)",
        "New content types use hash-based incremental update (existing behavior preserved)",
        "Embeddings triggerable via --embeddings flag on discover",
        "Typecheck passes"
      ],
      "priority": 11,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-012",
      "title": "Add --relations-only flag and update status command",
      "description": "As a DomainLens user, I want a --relations-only flag on discover that regenerates relations.md from existing skills without re-running extraction. I also want the status command to show business_rules, technical_rules, domain_map, relations status, and watch status. --relations-only should error with a clear message if no domain skills exist yet.",
      "acceptanceCriteria": [
        "--relations-only flag on discover skips schema, code scan, enrichment, and embeddings",
        "--relations-only errors with message if no domain skills exist",
        "Status output shows: business_rules count, technical_rules count, domain_map status, relations.md checkmark/status, watch status",
        "Status example matches PRD format",
        "Typecheck passes"
      ],
      "priority": 12,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-013",
      "title": "Add agent_batch_size and agent_strategy config fields",
      "description": "As a developer configuring DomainLens, I want two new config fields — agent_batch_size (files per sub-agent, default 40) and agent_strategy ('single' | 'multi', default 'multi') — so that I can control whether the agent uses a single session or the multi-session exploration strategy.",
      "acceptanceCriteria": [
        "DomainLensConfig type in src/types.ts includes agent_batch_size?: number and agent_strategy?: 'single' | 'multi'",
        "init command generates config.json with agent_batch_size: 40 and agent_strategy: 'multi'",
        "Both fields are documented in the config table comment block",
        "Typecheck passes"
      ],
      "priority": 13,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-014",
      "title": "Implement mapping phase (Phase 1 of multi-session exploration)",
      "description": "As a developer implementing multi-session exploration, I want a lightweight mapping function that runs a short LLM session to list directories and glob files, producing file groups partitioned by module/directory — without reading any file content — so the total cost is ~1-2k tokens.",
      "acceptanceCriteria": [
        "New function buildFileGroups(config, projectPath) exists in src/agent/",
        "Uses a short LLM call that only calls list_directory and glob_files (no read_file)",
        "Agent session terminates early once it has enough file paths (no finish() needed — just returns file paths from tool call history)",
        "Falls back to a simple walkdir grouping (by top-level code_path subdirectory) if the LLM call fails",
        "Returns string[][] where each inner array is a batch of file paths up to agent_batch_size",
        "Respects ignore list from config",
        "Typecheck passes"
      ],
      "priority": 14,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-015",
      "title": "Add file_filter support to agent runner for sub-agent sessions",
      "description": "As a developer implementing multi-session exploration, I want the agent runner to accept an optional file_filter (string[]) parameter that restricts read_file calls to only the specified paths, so each sub-agent only explores its assigned batch and cannot drift into other modules.",
      "acceptanceCriteria": [
        "runAgent() accepts an optional file_filter?: string[] in RunAgentOptions",
        "When file_filter is set, read_file calls for paths not in the filter return an error string: 'Error: file not in assigned batch'",
        "list_directory and glob_files are not restricted (the agent can still navigate to find its files)",
        "System prompt includes the assigned file list when file_filter is set: 'You are assigned to explore these files: ...'",
        "Typecheck passes"
      ],
      "priority": 15,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-016",
      "title": "Implement consolidation of multi-agent concept results",
      "description": "As a developer implementing multi-session exploration, I want a consolidation function that merges AgentConcept[] arrays from multiple sub-agent sessions — deduplicating by normalized name, merging signals and related_concepts, and resolving cross-module relations — so the pipeline receives a single unified list.",
      "acceptanceCriteria": [
        "New function consolidateConcepts(batches: AgentConcept[][]): AgentConcept[] exists in src/agent/",
        "Deduplication: concepts with the same name (case-insensitive) are merged into one",
        "Merged concept: signals arrays are concatenated and deduplicated by value",
        "Merged concept: related_concepts arrays are unioned",
        "Merged concept: definition from the batch with more signals wins; other definition appended as alternate if substantially different",
        "Merged concept: states and business_rules arrays are unioned",
        "Typecheck passes"
      ],
      "priority": 16,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-017",
      "title": "Wire multi-session strategy into agent runner dispatch",
      "description": "As a DomainLens user running discover --agent on a large codebase, I want the agent runner to automatically use the multi-session strategy (Phase 1 mapping → Phase 2 sub-agents → Phase 3 consolidation) when agent_strategy is 'multi', and fall back to the existing single-session flow when it is 'single'.",
      "acceptanceCriteria": [
        "runAgent() checks config.agent_strategy; routes to multi-session flow when 'multi' (default), single-session flow when 'single'",
        "Multi-session flow: calls buildFileGroups(), then runAgent() with file_filter per group, then consolidateConcepts()",
        "Progress output shows phase headers: '▶ Phase 1: Mapping files...', '▶ Phase 2: Exploring batch N/M...', '▶ Phase 3: Consolidating results...'",
        "If buildFileGroups() fails or returns a single group, falls back to single-session silently",
        "agent_max_context_tokens still applies per sub-session (not globally)",
        "forceSummarize() is still invoked per sub-session when its context limit is hit",
        "Typecheck passes"
      ],
      "priority": 17,
      "passes": true,
      "notes": ""
    }
  ]
}
